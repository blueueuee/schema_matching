# Valentine Benchmark Dataset Placeholder

This directory is designated for external benchmark datasets from the Valentine schema-matching benchmark suite (e.g. Valentine Wikidata, ChFin, OpenData benchmarks).

CSV pairs or JSON schemas placed here can be ingested into the benchmark harness via `evaluation.benchmark.run_benchmark()`.
